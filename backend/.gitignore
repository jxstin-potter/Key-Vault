# Node
node_modules/
.env
.env.*
# The template carries no secrets and is the documentation for what a .env
# needs to contain, so it is the one exception.
!.env.example
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Logs
logs
*.log

# Prisma
prisma/dev.db
prisma/dev.db-journal
